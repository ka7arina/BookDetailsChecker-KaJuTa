import {Text, View} from "react-native";
import React from "react";

// @ts-ignore
export default function ViewBookScreen({ navigation }) {

    const seeDetails = () => {
        // navigation.navigate('');
    }

    return (
        <View >
            <Text> this is a temporary page </Text>
        </View>
    )
}